# 1. Salvar como fix_dashboard.sh
echo '#!/bin/bash
# Fix rápido dashboard PNCP
echo "🔧 Corrigindo dashboard PNCP..."

# Renomear arquivo
if [ -f "dados_pncp.json" ]; then
    cp dados_pncp.json dados.json
    echo "✅ dados_pncp.json → dados.json"
else
    echo "❌ dados_pncp.json não encontrado!"
    exit 1
fi

# Validar JSON
python3 -m json.tool dados.json > /dev/null && echo "✅ JSON válido!" || echo "❌ JSON inválido!"

echo "🎉 Dashboard pronto! Recarregue a página."
' > fix_dashboard.sh

chmod +x fix_dashboard.sh
./fix_dashboard.sh
